# Cloudflare Pages Redirects
# SPA routing is natively handled by Cloudflare Pages (via 200.html / not_found_handling).
# Static assets, including /.well-known/assetlinks.json, are served directly without redirection.
